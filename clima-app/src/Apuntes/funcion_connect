// https://codepen.io/anon/pen/vQexwB?editors=0012
const connect = (v1,v2) => {
  console.log(v1)
  console.log(v2)
  
  //retorna una funcion ()=>{''}
  return (componente) => {
    const result = `${componente}-${v1*v2}`;
    console.log(result);
    return result;
  }
}
//Sin funciones!!!!
const componenteConectado = connect(4,5)('App');

componenteConectado;

// Imprime :
4
5
"App-20"




